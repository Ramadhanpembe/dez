[project]
name = "airflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ramadhanpembe", email = "63413802+Ramadhanpembe@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pendulum>=3.2.0",
]

[project.scripts]
airflow = "airflow:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
